/*
This fails because the radius can only be set on a circle. Box has no radius.
It even prints the nearby lines and displays what line has the error.
(This statement is ignored by the interpreter and the program should continues to run ("make run" may abort though))
*/

Begin a function called Test.

	Create a Box called box.
	Set the radius of box to 50.

	Print "Hello World!" to the console.
Leave the function.