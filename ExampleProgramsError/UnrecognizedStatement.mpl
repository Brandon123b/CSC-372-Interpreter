/*
This fails because there is an unrecognized statement that fails to be parced. 
It even prints the nearby lines and displays what line has the error.
(This statement is ignored by the interpreter and the program should continues to run ("make run" will abort though))
*/

Begin a function called Test.

	unrecognized statement!!!.

	Print "Hello World!" to the console.
Leave the function.