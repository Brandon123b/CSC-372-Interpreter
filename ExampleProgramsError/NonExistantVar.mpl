/*
This fails because the variable is not declared.
It prints the line number and the error message.
(This statement is ignored by the interpreter and the program should continues to run ("make run" will abort though))
*/


Begin a function called Test.

	Create a Box called box.
	Set the size of (non-existantVar) to 50 and 50.

	Print "Hello World!" to the console.
Leave the function.